type User @model {
  id: String!
  name: String!
  cards: [Card] @connection(keyName: "byUser", fields:["id"])
}
type Card @model
  @key(name:"byUser", fields: ["name", "facebook"]) {
  id: String!
  name: String!
  facebook: String
}
